language: cpp

compiler:
  - clang
  - gcc

os: linux
dist: bionic

addons:
  apt:
    packages:
      libboost-dev
      cmake

jobs:
  include:
    os: osx
    osx_image: xcode12.3
    compiler: clang
    addons:
      homebrew:
        packages:
          - boost

script:
  - git clone https://github.com/toruniina/toml11
  - ./build_sundials
  - cp Makefile.local.travisci Makefile.local
  - make
  - make test
